# dependencies
node_modules

# next.js build output
.next
out

# local tooling / editor
.claude
**/.DS_Store

# pnpm regenerates this locally with a non-portable build-approval stub;
# the site is a single package (no workspace), so it must not be committed.
pnpm-workspace.yaml

# logs
*.log

# env
.env*
!.env.example

# next typegen (regenerated on build)
next-env.d.ts
